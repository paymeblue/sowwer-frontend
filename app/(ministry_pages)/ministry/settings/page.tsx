import { Metadata } from "next";
import SettingsPage from "./settings";

export const metadata: Metadata = { title: "Settings - Ministry | Soower" };

const Settings = () => {
  return <SettingsPage />;
};

export default Settings;
